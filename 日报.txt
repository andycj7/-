曹杰 2020.7.13 日报

今日实习内容：
1、学习html基本语法，常用标签以及相关属性
2、学习JavaScript的基本语法，对象的构造，函数使用，对象的常用封装方法

曹杰 2020.7.14 日报
今日实习内容：
1、学习JavaScript,ES6特性，包括箭头函数，let/const声明块作用域变量，for of循环等；
2、学习promise对象，进行异步操作，避免耗时的操作阻塞主线程；
3、学习JS提供的字符串方法，search(),indexof()等；
4、配置cygwin，使用cmake，编译静态库和动态库。

心得体会：
1、for-in循环和for-of 循环不同，for-in中的取出的是索引，for-of中取出的是元素。
2、箭头函数在使用时，有很多注意点，如：没有自己的this，不适合定义对象方法；未被提升，需要在使用前定义。
3、在CMakeLists中，如果指定头文件路径，在编译库文件时，通常把头文件和源文件放在同一个文件夹，并且在源文件中使用双引号("")，引入头文件，表示在当前文件夹下搜索。

曹杰 2020.7.15 日报
今日实习内容：
1、使用学习的HTML和JS知识，完成5道基本练习题。
2、学习CSS样式表，flex布局。

心得体会：
1、样式表控件的百分比大小，是根据父控件的大小来的，一般要确定一个最上层的父控件的高度。
2、在innerHTML的字符串中的标签，会被解释为正常的标签，字符串中的"\n"等转移字符无效。
3、有一些非标准.mp4在播放时，可能会出现没有画面的情况，需要转化为标准的MP4 avc编码。

曹杰 2020.7.16 日报
今日实习内容：
1、把5道web题，在工程机中跑一遍
2、完成了视频播放器的题目，在工程机中，看了一下效果，适当修改了布局
3、在emss开发者社区中，看了emss.mk的写法，编译选项。
4、参加了JS插件开发的培训，初步了解一些流程

心得体会:
1、对于布局大小，工程机中和电脑网页中，不一样，需要调整。
2、div标签可以用作块状布局+flex布局，很方便。

曹杰 2020.7.17
今日实习内容：
1、使用base_plugin，把C++库编译，以及JS调用C++函数，流程操作了一遍
2、写关于文件读写的题目

心得体会：
1、使用emss ndk的atelier_plugin，编译无法通过，不清楚原因
2、JS层的主要就是，通过addLister注册回调，exec调用C++模块，C++层onMessage接受exec传递的参数，PostMessage传回JS层

曹杰 2020.7.20
今日实习内容：
1、使用GeoBOS的接口，实现对C++函数的调用
2、github上下载了rapaidjson的源码，添加头文件到工程中，libbase_plguin中应该编译好了源码
3、写文件系统函数，在工程机上显示目录
4、配置adb，显示手机目录

心得体会：
1、GeoBOS在接受C++的传入的数据时，当正确的调用时，会按json字符串数组解析，C++在post时必须以json格式
2、回调函数中第一个参数是err错误信息，接下来依次是解析得到的json字符串，在不定个时可以使用argument接受

曹杰 2020.7.21
今日实习内容：
1、学习写软件需求分析，写了一分部
2、根据写的需求分析，规范了原来写的软件接口
3、简化原有C++代码，继续编写接口

心得体会
没想好直接写代码，可以有很多考虑不周的地方，并且接口定义也会很乱


曹杰 2020.7.21
今日实习内容：
1、写完软件需求分析，完善需求分析中的功能
2、画了用例图和类图
3、完善了软件的界面，目录的跳转，以及不同界面间的切换，测试软件功能，修改bug

心得体会
1、在写软件时，很容易因为符号的问题而出错
2、函数测试时，尽可能在可能出错的地方，多打日志，减少重新编译的麻烦