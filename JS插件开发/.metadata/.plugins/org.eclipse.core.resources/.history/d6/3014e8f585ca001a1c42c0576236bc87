GeoBOS.filesystem = {
		
	init : function (cb) {
		GeoBOS.plugin.execute("FileSystemPlugin", "init", GeoBOS.getId(), '', null, cb);
	},
	open : function (cb, object) {
		GeoBOS.plugin.execute("FileSystemPlugin", "open", GeoBOS.getId(), object.innerHTML, null, cb);
	}
}