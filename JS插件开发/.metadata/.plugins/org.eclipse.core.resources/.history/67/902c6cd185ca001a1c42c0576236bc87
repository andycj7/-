GeoBOS.filesystem = {
		
	init : function (cb) {
		GeoBOS.plugin.execute("FileSystemPlugin", "init", GeoBOS.getId(), '', null, cb);
	},
	open : function (cb, ob) {
		GeoBOS.plugin.execute("FileSystemPlugin", "open", GeoBOS.getId(), , null, cb);
	}
}